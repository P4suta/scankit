# SPDX-FileCopyrightText: 2026 scankit contributors
#
# SPDX-License-Identifier: MIT OR Apache-2.0

# The toolchain every gate runs on. Tool versions are pinned in mise.toml.

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
